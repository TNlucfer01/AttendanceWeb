# ============================================
# Attendance Web App — Application Config
# ============================================

# --- Server ---
server.port=8080

# --- MySQL / MariaDB Database ---
spring.datasource.url=jdbc:mariadb://localhost:3306/attendancedb
spring.datasource.driverClassName=org.mariadb.jdbc.Driver
spring.datasource.username=attendance
spring.datasource.password=Attendance@123

# --- JPA / Hibernate ---
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# --- JWT ---
app.jwt.secret=AttendanceApp2026SecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLong!!
app.jwt.expiration-ms=1800000

# --- App Defaults ---
app.admin.email=admin@attendance.com
app.admin.password=Admin@123

# --- Thymeleaf ---
spring.thymeleaf.check-template-location=false
